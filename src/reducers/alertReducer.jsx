import { types } from "../types";

const initialState = {
    alerta:null
}

export const alertReducer = (state = initialState, action) => {
    switch (action.type) {    
        default:
            return state;
    }
}